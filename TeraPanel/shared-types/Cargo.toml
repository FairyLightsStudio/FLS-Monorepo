[package]
name = "shared-types"
version = "0.1.0"
publish = false
edition.workspace = true
build = "build.rs"

[build-dependencies]
pilota-build.workspace = true
# env_logger = "0.11" # 添加这个
# log = "0"

[dependencies]
pilota.workspace = true
